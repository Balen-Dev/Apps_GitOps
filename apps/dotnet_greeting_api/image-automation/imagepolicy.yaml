apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: dotnet-greeting-api
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: dotnet-greeting-api
  policy:
    semver:
      range: "*"
