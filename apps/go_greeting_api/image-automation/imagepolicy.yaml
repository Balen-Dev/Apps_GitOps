apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: go-greeting-api
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: go-greeting-api
  policy:
    semver:
      range: "*"
